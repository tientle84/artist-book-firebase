import React from "react";

class PlaylistItem {
    constructor(title, uri, isVideo) {
        this.title = title;
        this.uri = uri;
        this.isVideo = isVideo;
    }
}

export default PlaylistItem;
